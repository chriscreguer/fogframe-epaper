# your configuration — holds your location, never commit it
config.toml

# secrets — never commit
.env
dropbox.env
dropbox_refresh.json
firmware/fogframe/secrets.h

# baked for your location — regenerate with make_base_map.py / make_labels.py
assets/map_base.png
assets/labels.json
.tilecache/

# python
__pycache__/
*.pyc
venv/
.venv/

# local build artifacts (CI commits device/ and world/)
build/
.DS_Store
